<head>
  <link rel="stylesheet" href="/css/updateInformation.css">
</head>

<div class="information-background">
  <div class="container-information-header">
    <center>
      <h1>Create Dressing</h1>
      <h4>For admin only</h4>
    </center>
  </div>
  <div class="container-update-item">
    <div class="container-fluid item">
      <form id="createDressing">
        <div class="container py-3">
          <h2 class="">Dressings</h2>
          <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#collapsable-catgeory-list" aria-expanded="false" aria-controls="collapsable-catgeory-list">
            See all active dressings
          </button>
          <div class="collapse" id="collapsable-catgeory-list">
            <div class="card card-body">
              <ul>
                {{#each d as |x|}}  
                  <li>{{x.name}}</li>           
                {{/each}}
              </ul>
            </div>
          </div>
          <br><br>
          <div class="forms">
            <label 
              for="menu-item-name" 
              class="form-label"
            >
              <b>Name</b>
              <b style="color:#FF0000";>*required</b>
            </label>
            <input 
              id="name" 
              type="text" 
              class="form-control" 
              value="{{d.name}}" 
              placeholder="Some Name..."
              required
            >
            <label 
              for="menu-item-description" 
              class="form-label"
            >
              <b>Price 3 OZ</b>
              <b style="color:#FF0000";>*required</b>
            </label>
            <input 
              id="price3oz" 
              type="text" 
              class="form-control" 
              value="{{d.price3oz}}" 
              placeholder="0.00..."
              require
            >
            <label 
              for="menu-item-description" 
              class="form-label"
            >
              <b>Price 16 OZ</b>
              <b style="color:#FF0000";>*required</b>
            </label>
            <input 
              id="price16oz" 
              type="text" 
              class="form-control" 
              value="{{d.price16oz}}" 
              placeholder="0.00..."
              require
            >
            <br><br>
          </div>	
          <div class="d-flex justify-content-evenly">
            <button
              type="submit"
              class="btn btn-primary">
                Create
            </button>
            <button 
              class="btn btn-warning view-menu-button" onclick="history.back()"
            >
              Cancel
            </button>
					</div>
        </div>
      </form>
    </div>
  </div>

  <br><br><br><br><br><br><br><br>
</div>

<script src="/js/createDressing.js"></script>